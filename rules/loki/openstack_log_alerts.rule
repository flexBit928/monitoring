groups:
  - name: openstack-logs
    rules:
      - alert: CriticalErrorInLogs
        expr: count_over_time({} |= "CRITICAL" [5m]) > 5
        for: 1m
        labels:
          severity: critical
        annotations:
          summary: "Critical errors detected in OpenStack logs"
