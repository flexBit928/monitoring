groups:
- name: grafana-agent-high-log-volume
  rules:
  - alert: HighLogVolume
    expr: |
      count_over_time(({job=~".+"})[30s]) > 100000
    labels:
      severity: warning
    annotations:
      summary: "Log rate is too high - over 100000 log lines in 30s"
